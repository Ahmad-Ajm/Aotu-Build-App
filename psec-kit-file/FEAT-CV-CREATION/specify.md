# مواصفات تفصيلية: إنشاء السيرة الذاتية (FEAT-CV-CREATION)

## 📋 نظرة عامة
ميزة تسمح للمستخدمين المصادقين بإنشاء وتعديل وتصدير سيرتهم الذاتية من خلال واجهة سهلة الاستخدام.

## 🎯 المتطلبات الوظيفية

### FR-CV-001: إنشاء سيرة ذاتية جديدة
**الوصف**: يجب أن يتمكن المستخدم المصادق من إنشاء سيرة ذاتية جديدة.
**المدخلات**:
- عنوان السيرة الذاتية (إلزامي)
- اختيار القالب (اختياري، افتراضي: قالب 1)

**المخرجات**:
- سيرة ذاتية جديدة مع معرف فريد
- توجيه إلى صفحة التحرير

**القواعد**:
- يجب أن يكون المستخدم مصادقاً
- يمكن للمستخدم إنشاء عدد غير محدود من السير الذاتية
- كل سيرة ذاتية يجب أن يكون لها عنوان فريد للمستخدم

### FR-CV-002: تحرير المعلومات الشخصية
**الوصف**: إدخال وتعديل المعلومات الشخصية.
**الحقول**:
- الاسم الكامل (إلزامي)
- البريد الإلكتروني (إلزامي، التحقق من الصيغة)
- رقم الهاتف (اختياري)
- العنوان (اختياري)
- الموقع الجغرافي (اختياري)
- صورة شخصية (اختياري، الحد الأقصى: 5MB، الصيغ: JPG, PNG)

**التحقق**:
- البريد الإلكتروني يجب أن يكون بصيغة صحيحة
- رقم الهاتف يجب أن يكون بصيغة دولية

### FR-CV-003: إدارة الخبرات العملية
**الوصف**: إضافة وتعديل وحذف الخبرات العملية.
**الحقول لكل خبرة**:
- المسمى الوظيفي (إلزامي)
- الشركة (إلزامي)
- الموقع (اختياري)
- تاريخ البدء (إلزامي)
- تاريخ الانتهاء (اختياري، إذا فارغ: "حتى الآن")
- الوصف (اختياري، الحد الأقصى: 1000 حرف)

**القواعد**:
- يمكن إضافة عدد غير محدود من الخبرات
- الترتيب قابل للتعديل بالسحب والإفلات
- التاريخ يجب أن يكون بصيغة YYYY-MM

### FR-CV-004: إدارة التعليم والمؤهلات
**الوصف**: إضافة وتعديل وحذف المؤهلات التعليمية.
**الحقول لكل مؤهل**:
- الدرجة العلمية (إلزامي)
- المؤسسة التعليمية (إلزامي)
- التخصص (اختياري)
- تاريخ البدء (إلزامي)
- تاريخ التخرج (اختياري)
- الوصف (اختياري، الحد الأقصى: 500 حرف)

### FR-CV-005: إدارة المهارات
**الوصف**: إضافة وتعديل وحذف المهارات.
**الحقول لكل مهارة**:
- اسم المهارة (إلزامي)
- المستوى (اختياري: مبتدئ، متوسط، متقدم، خبير)
- السنوات (اختياري)

**القواعد**:
- يمكن إضافة مهارات بدون ترتيب محدد
- يمكن حذف المهارات بسهولة

### FR-CV-006: معاينة السيرة الذاتية
**الوصف**: عرض معاينة مباشرة أثناء التحرير.
**المتطلبات**:
- تحديث المعاينة تلقائياً عند كل تغيير
- عرض جميع الأقسام المكتملة
- إخفاء الأقسام غير المكتملة (اختياري)
- تبديل بين وضع المعاينة ووضع التحرير

### FR-CV-007: حفظ التعديلات
**الوصف**: حفظ التعديلات تلقائياً ويدوياً.
**القواعد**:
- حفظ تلقائي كل 30 ثانية
- زر حفظ يدوي
- إشعار عند نجاح الحفظ
- استعادة البيانات في حالة فقدان الاتصال

### FR-CV-008: تصدير السيرة الذاتية
**الوصف**: تصدير السيرة الذاتية بصيغ مختلفة.
**الصيغ المدعومة**:
- PDF (إلزامي)
- DOCX (اختياري)

**خيارات التصدير**:
- اختيار القالب
- اختيار اللغة (عربي/إنجليزي)
- تضمين الصورة الشخصية (نعم/لا)
- تضمين المعلومات الحساسة (نعم/لا)

### FR-CV-009: إدارة القوالب
**الوصف**: اختيار وتخصيص القوالب.
**القوالب الافتراضية**:
1. قالب احترافي (كلاسيكي)
2. قالب مبدع (حديث)
3. قالب بسيط (مينيماليست)

**خيارات التخصيص**:
- لون الأساسي
- نوع الخط
- حجم الخط
- تباعد الأسطر

## 🏗️ المتطلبات غير الوظيفية

### NFR-CV-001: الأداء
- وقت تحميل صفحة التحرير: < 2 ثانية
- وقت حفظ التعديلات: < 1 ثانية
- وقت التصدير إلى PDF: < 30 ثانية
- دعم حتى 1000 حرف في الوصف

### NFR-CV-002: الأمان
- المصادقة مطلوبة لجميع العمليات
- كل مستخدم يرى سيرته الذاتية فقط
- تشفير البيانات الحساسة في قاعدة البيانات
- التحقق من صلاحيات الوصول على مستوى الـ API

### NFR-CV-003: التوافقية
- دعم أحدث إصدارات Chrome, Firefox, Safari
- دعم الأجهزة المحمولة (Responsive Design)
- دعم اللغة العربية والإنجليزية
- دعم وضع عدم الاتصال (القراءة فقط)

### NFR-CV-004: سهولة الاستخدام
- واجهة بديهية وسهلة الاستخدام
- إرشادات واضحة لكل حقل
- معاينة مباشرة
- حفظ تلقائي

## 🎨 واجهة المستخدم

### صفحة إنشاء سيرة ذاتية جديدة
```
[زر: إنشاء سيرة ذاتية جديدة]
└── [مودال: إنشاء سيرة ذاتية]
    ├── حقل نصي: العنوان
    ├── قائمة منسدلة: اختيار القالب
    ├── زر: إنشاء
    └── زر: إلغاء
```

### صفحة تحرير السيرة الذاتية
```
[شريط جانبي]          [منطقة المحتوى]
├── الأقسام:          ├── معلومات شخصية
│   ├── معلومات شخصية │   ├── الاسم الكامل
│   ├── خبرات عمل     │   ├── البريد الإلكتروني
│   ├── التعليم       │   ├── رقم الهاتف
│   └── المهارات      │   └── ...
├── الإعدادات:        ├── خبرات عمل
│   ├── القالب        │   ├── [قائمة الخبرات]
│   ├── التصدير       │   └── زر: إضافة خبرة
│   └── الحذف         └── ...
└── المعاينة:         [منطقة المعاينة]
    └── زر: معاينة    │   └── معاينة السيرة الذاتية
```

### مكونات واجهة المستخدم
1. **CVEditorComponent**: المكون الرئيسي للتحرير
2. **PersonalInfoComponent**: تحرير المعلومات الشخصية
3. **WorkExperienceComponent**: إدارة الخبرات
4. **EducationComponent**: إدارة التعليم
5. **SkillsComponent**: إدارة المهارات
6. **PreviewComponent**: معاينة السيرة الذاتية
7. **TemplateSelectorComponent**: اختيار القالب
8. **ExportModalComponent**: نافذة التصدير

## 🔧 واجهة برمجة التطبيقات (API)

### إنشاء سيرة ذاتية جديدة
```http
POST /api/cv
Authorization: Bearer {token}
Content-Type: application/json

{
  "title": "سيرتي الذاتية",
  "template": "professional"
}
```

### تحديث السيرة الذاتية
```http
PUT /api/cv/{id}
Authorization: Bearer {token}
Content-Type: application/json

{
  "personalInfo": { ... },
  "workExperience": [ ... ],
  "education": [ ... ],
  "skills": [ ... ]
}
```

### الحصول على السيرة الذاتية
```http
GET /api/cv/{id}
Authorization: Bearer {token}
```

### تصدير إلى PDF
```http
POST /api/cv/{id}/export/pdf
Authorization: Bearer {token}
Content-Type: application/json

{
  "template": "professional",
  "language": "ar",
  "includePhoto": true,
  "includeSensitiveInfo": false
}
```

## 🗄️ نموذج البيانات

### جدول CVs
```sql
CREATE TABLE CVs (
    Id UUID PRIMARY KEY,
    UserId UUID NOT NULL,
    Title VARCHAR(200) NOT NULL,
    PersonalInfo JSONB,
    WorkExperience JSONB,
    Education JSONB,
    Skills JSONB,
    Template VARCHAR(50) DEFAULT 'professional',
    IsPublic BOOLEAN DEFAULT false,
    CreatedAt TIMESTAMP DEFAULT NOW(),
    UpdatedAt TIMESTAMP DEFAULT NOW(),
    FOREIGN KEY (UserId) REFERENCES Users(Id)
);

CREATE INDEX IX_CVs_UserId ON CVs(UserId);
CREATE INDEX IX_CVs_IsPublic ON CVs(IsPublic) WHERE IsPublic = true;
```

### هياكل JSON
```json
{
  "personalInfo": {
    "fullName": "أحمد محمد",
    "email": "ahmad@example.com",
    "phone": "+966500000000",
    "address": "الرياض، السعودية",
    "photoUrl": "/uploads/photos/photo.jpg"
  },
  "workExperience": [
    {
      "id": "exp1",
      "jobTitle": "مطور برمجيات",
      "company": "شركة التقنية",
      "location": "الرياض",
      "startDate": "2020-01",
      "endDate": "2023-12",
      "description": "تطوير تطبيقات ويب..."
    }
  ]
}
```

## 🔍 سيناريوهات الاختبار

### اختبار إنشاء سيرة ذاتية
1. تسجيل الدخول كمستخدم
2. النقر على "إنشاء سيرة ذاتية جديدة"
3. إدخال عنوان واختيار قالب
4. التحقق من إنشاء السيرة الذاتية

### اختبار تحرير المعلومات
1. فتح سيرة ذاتية موجودة
2. تحديث المعلومات الشخصية
3. التحقق من حفظ التعديلات
4. التحقق من تحديث المعاينة

### اختبار التصدير
1. فتح سيرة ذاتية مكتملة
2. النقر على "تصدير إلى PDF"
3. اختيار الخيارات
4. التحقق من تحميل الملف

## 📊 مقاييس المراقبة
1. عدد السير الذاتية المنشأة يومياً
2. متوسط وقت إنشاء السيرة الذاتية
3. معدل إكمال السير الذاتية
4. أخطاء حفظ البيانات
5. وقت استجابة واجهة برمجة التطبيقات