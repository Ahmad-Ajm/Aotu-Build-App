# خطة التنفيذ: إنشاء السيرة الذاتية (FEAT-CV-CREATION)

## 📋 نظرة عامة
تنفيذ نظام متكامل لإنشاء وإدارة السير الذاتية يسمح للمستخدمين بإنشاء وتعديل وحفظ سيرهم الذاتية بسهولة.

## 🎯 الأهداف الرئيسية
1. إنشاء واجهة مستخدم سهلة لإنشاء السيرة الذاتية
2. تنفيذ منطق الأعمال لحفظ وتحديث البيانات
3. ضمان أمان البيانات والخصوصية
4. دعم تعدد اللغات (العربية والإنجليزية)

## 🏗️ الهيكل التقني

### Backend (.NET 8 + ABP Framework)
1. **CV Aggregate Root**
   - `Cv` entity مع جميع الخصائص الأساسية
   - `CvSection` entity للأقسام المختلفة
   - `CvTemplate` entity للقوالب الجاهزة

2. **Services**
   - `ICvAppService` للعمليات الأساسية (Create, Update, Delete, Get)
   - `ICvExportService` لتصدير السير الذاتية
   - `ICvTemplateService` لإدارة القوالب

3. **Repositories**
   - `ICvRepository` للوصول إلى قاعدة البيانات
   - `ICvTemplateRepository` لقوالب السير الذاتية

### Frontend (Angular 17)
1. **Modules**
   - `CvModule` يحتوي على جميع مكونات السيرة الذاتية
   - `SharedModule` للمكونات المشتركة

2. **Components**
   - `CvCreateComponent` لإنشاء سيرة ذاتية جديدة
   - `CvEditComponent` لتعديل سيرة ذاتية موجودة
   - `CvPreviewComponent` لعرض معاينة السيرة الذاتية
   - `CvListComponent` لعرض قائمة السير الذاتية

3. **Services**
   - `CvService` للاتصال مع Backend API
   - `CvTemplateService` لإدارة القوالب

## 📊 قاعدة البيانات (PostgreSQL)

### الجداول الرئيسية
```sql
-- جدول السير الذاتية
CREATE TABLE Cvs (
    Id UUID PRIMARY KEY,
    UserId UUID NOT NULL,
    Title NVARCHAR(200) NOT NULL,
    Summary TEXT,
    Status INT NOT NULL, -- Draft, Published, Archived
    TemplateId UUID,
    LanguageCode NVARCHAR(10) DEFAULT 'ar',
    CreatedAt TIMESTAMP NOT NULL,
    UpdatedAt TIMESTAMP NOT NULL,
    FOREIGN KEY (UserId) REFERENCES AbpUsers(Id),
    FOREIGN KEY (TemplateId) REFERENCES CvTemplates(Id)
);

-- جدول أقسام السيرة الذاتية
CREATE TABLE CvSections (
    Id UUID PRIMARY KEY,
    CvId UUID NOT NULL,
    SectionType INT NOT NULL, -- PersonalInfo, Education, Experience, Skills, etc.
    Title NVARCHAR(200) NOT NULL,
    Content JSONB, -- لتخزين البيانات المرنة
    DisplayOrder INT NOT NULL,
    CreatedAt TIMESTAMP NOT NULL,
    FOREIGN KEY (CvId) REFERENCES Cvs(Id) ON DELETE CASCADE
);

-- جدول قوالب السير الذاتية
CREATE TABLE CvTemplates (
    Id UUID PRIMARY KEY,
    Name NVARCHAR(200) NOT NULL,
    Description TEXT,
    PreviewImageUrl NVARCHAR(500),
    TemplateData JSONB NOT NULL, -- هيكل القالب
    IsActive BOOLEAN DEFAULT TRUE,
    CreatedAt TIMESTAMP NOT NULL
);
```

## 🔄 سير العمل (Workflow)

### إنشاء سيرة ذاتية جديدة
1. المستخدم يختار قالباً أو يبدأ من الصفر
2. إدخال المعلومات الأساسية (الاسم، العنوان، الملخص)
3. إضافة الأقسام (التعليم، الخبرة، المهارات، إلخ)
4. معاينة السيرة الذاتية
5. حفظ كمسودة أو نشر مباشرة

### تعديل سيرة ذاتية موجودة
1. عرض السيرة الذاتية في وضع التحرير
2. تعديل المحتوى أو إضافة أقسام جديدة
3. حفظ التعديلات
4. معاينة التغييرات

## 🎨 تصميم الواجهة

### الصفحات الرئيسية
1. **صفحة إنشاء سيرة ذاتية**
   - اختيار القالب
   - نموذج إدخال المعلومات
   - محرر WYSIWYG للمحتوى
   - معاينة مباشرة

2. **صفحة تعديل السيرة الذاتية**
   - عرض جميع الأقسام
   - إمكانية إعادة الترتيب
   - حفظ تلقائي

3. **صفحة معاينة السيرة الذاتية**
   - عرض كامل للسيرة الذاتية
   - خيارات التصدير (PDF، Word)
   - مشاركة عبر الرابط

## 🔒 الأمان والصلاحيات
- التحقق من هوية المستخدم قبل الوصول إلى السير الذاتية
- صلاحيات CRUD كاملة للمالك فقط
- تشفير البيانات الحساسة
- تسجيل أنشطة المستخدمين

## 📈 مقاييس النجاح
- وقت إنشاء سيرة ذاتية جديد أقل من 10 دقائق
- معدل استخدام الميزة > 80% من المستخدمين المسجلين
- معدل رضا المستخدمين > 4/5
- عدد السير الذاتية المنشورة شهرياً

## ⏱️ الجدول الزمني المقترح
- **الأسبوع 1-2**: تصميم قاعدة البيانات و Backend Entities
- **الأسبوع 3-4**: تطوير Services و APIs
- **الأسبوع 5-6**: تطوير Frontend Components
- **الأسبوع 7-8**: التكامل والاختبار
- **الأسبوع 9**: المراجعة والتعديلات
- **الأسبوع 10**: النشر والتوثيق

## 🧪 الاختبار
- Unit Tests لجميع Services
- Integration Tests للـ APIs
- E2E Tests لسير العمل الكامل
- اختبارات الأداء والتحميل

## 📚 التوثيق
- توثيق الـ APIs باستخدام Swagger
- دليل المستخدم خطوة بخطوة
- أمثلة على الاستخدام
- استكشاف الأخطاء وإصلاحها